/*
Create a struct called "Date" with three members: day, month, year(all integers). Write a function to compare to two date
structure and return 1 if the first date is later, -1 it it's earlier, and 0 if they are same.
*/

#include <stdio.h>

int cDate();

struct Date{
    int year;
    int month;
    int day;
};

int main(){
    struct Date dates[2];

    for (int i = 0; i < 2; i+){
        printf("Year: ");
        scanf("%d", &dates[i].year);

        printf("Month: ");
        scanf("%d", &dates[i].month);

        printf("Day: ");
        scanf("%d", &dates[i].day);
    }
    cDate(d1,d2);
    return 0;
}

int cDate(int d1, int d2){

    if (dates[i].year && dates[i]) 


}
